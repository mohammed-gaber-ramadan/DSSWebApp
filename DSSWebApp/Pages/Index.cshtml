@page
@model IndexModel
@{
    ViewData["Title"] = "Survey Form";
}

<style>
    /* Basic styling for the entire container */
    .container {
        font-family: 'Arial', sans-serif;
    }

    /* Styles for the interactive cards */
    .question-card {
        padding: 20px;
        border: 1px solid #e0e0e0;
        border-radius: 5px;
        margin-bottom: 20px;
        transition: all 0.3s ease-in-out;
    }

        .question-card:hover {
            transform: translateY(-5px);
            box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.1);
        }

    /* Radio button styles */
    .radio-option {
        margin: 5px 0;
        padding-left: 25px;
    }

    /* Initially hide the form */
    form {
        display: none;
    }
</style>

<div class="container mt-5">

    <div class="container mt-4 dss-intro">
    <!-- DSS Welcome Card -->
    <div class="card mb-4">
        <div class="card-header">
            Welcome to the Decision Support System (DSS)
        </div>
        <div class="card-body">
            <p class="card-text">
                This DSS is constructed to aid in selecting apt management procedures and tools tailored for teleworking software developers. Although still evolving, we are confident about its potential as a significant tool for project managers steering the intricacies of managing remote workforces.
            </p>
        </div>
    </div>
    
    <!-- How to Use DSS Card -->
    <div class="card mb-4">
        <div class="card-header">
            How to Use the DSS
        </div>
        <div class="card-body">
            <ol>
                <li>Initiate by answering the supplied survey queries.</li>
                <li>Upon survey completion, the DSS will scrutinize your data to pinpoint fitting procedures and tools.</li>
                <li>In sync with the workflow examination, the DSS will offer you personalized advice.</li>
            </ol>
            <p class="mt-3 text-warning">
                <strong>Note:</strong> As the DSS remains in its developmental phase, ensure you meticulously review its suggestions before their real-world deployment.
            </p>
        </div>
    </div>
    
    <!-- Feedback Card -->
    <div class="card mb-4">
        <div class="card-header">
            Your Feedback Matters
        </div>
        <div class="card-body">
            <p class="card-text">
                Post your project conclusion, we would be grateful for an evaluation of the DSS's proposals. Your insights are crucial. They will guide us in honing the system, making it increasingly precise and advantageous for future project managers.
            </p>
        </div>
    </div>
    
    <!-- Thank You Note -->
    <div class="text-center mt-4">
        <h5>Thank you for your active engagement and confidence in our system!</h5>
    </div>

        <!-- Start button -->
        <button class="btn btn-primary" onclick="startSurvey()">Start</button>
</div>

    <form method="post">
        @foreach (var question in Model.Questions)
        {
            <div class="question-card">
                <label for="@question.Id">@question.Text</label>
                @foreach (var option in question.Options)
                {
                    <div class="radio-option">
                        <input type="radio" name="@question.Id" value="@option" required>
                        <label>@option</label>
                    </div>
                }
            </div>
        }
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>

<!-- JavaScript function to hide introduction div and display form -->
<script>
    function startSurvey() {
        document.querySelector(".dss-intro").style.display = "none";
        document.querySelector("form").style.display = "block";
    }
</script>
