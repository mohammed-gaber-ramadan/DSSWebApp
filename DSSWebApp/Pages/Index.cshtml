@page
@model IndexModel
@{
    ViewData["Title"] = "Survey Form";
}

<style>
    /* Basic styling for the entire container */
    .container {
        font-family: 'Arial', sans-serif;
    }

    /* Styles for the interactive cards */
    .question-card {
        padding: 20px;
        border: 1px solid #e0e0e0;
        border-radius: 5px;
        margin-bottom: 20px;
        transition: all 0.3s ease-in-out;
    }

        .question-card:hover {
            transform: translateY(-5px);
            box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.1);
        }

    /* Radio button styles */
    .radio-option {
        margin: 5px 0;
        padding-left: 25px;
    }

    /* Initially hide the form */
    form {
        display: none;
    }
</style>

<div class="container mt-5">
    <div class="dss-intro">
        <h2>Welcome to the Decision Support System (DSS)</h2>
        <p>
            This DSS is designed to help you select suitable management processes and tools in the context of telework for software developers. While the system is still under development, we believe it can be a valuable tool for project managers navigating the challenges of managing telework teams.
        </p>

        <h4>How to Use the DSS:</h4>
        <ol>
            <li>Begin by answering the survey questions provided.</li>
            <li>After completing the survey, the DSS will analyze your inputs to identify suitable processes and tools.</li>
            <li>Based on the workflow analysis, the DSS will present you with tailored recommendations.</li>
        </ol>
        <p>
            <strong>Note:</strong> Given that the DSS is still a work-in-progress, it's essential to review the recommendations thoroughly before applying them in a real-world setting.
        </p>

        <h4>Your Feedback Matters:</h4>
        <p>
            Once you've concluded your project, we would appreciate it if you could evaluate the recommendations provided by the DSS. Your feedback is vital. It will help us refine the system, ensuring it becomes increasingly accurate and beneficial for project managers in the future.
        </p>

        <p>Thank you for your participation and trust in our system!</p>

        <!-- Start button -->
        <button class="btn btn-primary" onclick="startSurvey()">Start</button>
    </div>

    <form method="post">
        @foreach (var question in Model.Questions)
        {
            <div class="question-card">
                <label for="@question.Id">@question.Text</label>
                @foreach (var option in question.Options)
                {
                    <div class="radio-option">
                        <input type="radio" name="@question.Id" value="@option" required>
                        <label>@option</label>
                    </div>
                }
            </div>
        }
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>

<!-- JavaScript function to hide introduction div and display form -->
<script>
    function startSurvey() {
        document.querySelector(".dss-intro").style.display = "none";
        document.querySelector("form").style.display = "block";
    }
</script>
